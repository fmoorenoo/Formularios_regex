Crear basedatos
CREATE DATABASE dew
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

Seleccionar:
USE dew;

Crear tabla:
CREATE TABLE usuarios (
  dni         VARCHAR(20)  NOT NULL PRIMARY KEY,
  nombre      VARCHAR(100) NOT NULL,
  apellido    VARCHAR(150) NOT NULL,
  fecha       VARCHAR(20)  NOT NULL,
  cp          VARCHAR(10)  NOT NULL,
  correo      VARCHAR(150) NOT NULL,
  telefono    VARCHAR(20)  NOT NULL,
  movil       VARCHAR(20)  NOT NULL,
  tarjeta     VARCHAR(30)  NOT NULL,
  iban        VARCHAR(34)  NOT NULL,
  contrasena  VARCHAR(255) NOT NULL
);

Ruta proyecto (servidor XAMPP)
/opt/lampp/htdocs/

Mover carpeta
/opt/lampp/htdocs/Formularios_regex/
    index.html
    script.js
    datos.json
    process.php
    insertUser.php
    getUser.php


Permisos
sudo chmod -R 777 /opt/lampp/htdocs/Formularios_regex

Iniciar XAMPP
sudo /opt/lampp/lampp start


